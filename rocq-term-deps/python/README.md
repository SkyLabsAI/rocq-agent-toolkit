Python Bindings: `rocq-term-deps`
=================================

This package provides python bindings for the `rocq-term-deps` plugins, which enable querying
Rocq for the direct dependencies of a given identifier.
