(env
 (_
  (flags :standard %{dune-warnings})))

(rule
 (mode promote)
 (action
  (with-stdout-to README.md
   (progn
    (cat README.md.prefix)
    (echo "\n")
    (run rocq-doc-manager --api-docs)))))
