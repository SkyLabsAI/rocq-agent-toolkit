Supporting Python Library for the `jsonrpc-tp` OCaml Package
============================================================

This Python library is relied on by the code that is generate by the OCaml
API generator of the `jsonrpc-tp` OCaml package.
